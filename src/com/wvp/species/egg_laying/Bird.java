package com.wvp.species.egg_laying;

import com.wvp.models.Animal;

public class Bird extends Animal {
    public Bird(int id, String species, String nickname, int age, int weight, int speed, String gender) {
        super(id, species, nickname, age, weight, speed, gender);
    }
}
